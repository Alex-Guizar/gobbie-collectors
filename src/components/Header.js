import React from 'react';

const Header = () => {
  return (
    <header>
      <nav className="navbar navbar-dark bg-dark text-white">
        Gobbie Collectors
      </nav>
    </header>
  );
};

export default Header;
